apiVersion: v1
kind: Service
metadata:
  name: kbot-metrics
  namespace: flux-system
spec:
  selector:
    app.kubernetes.io/instance: kbot
    app.kubernetes.io/name: kbot
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
